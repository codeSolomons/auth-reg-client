function RegisterInfo(){
    return(
        <p className="Register-Info-text">Don't have an account yet? 
            <span className="Register-Info-text-blue"> Register</span>
        </p>
    );
}

export default RegisterInfo;